分析租房API接口，提供了哪些可供查询的接口以及每个接口必须的参数、可选的参数。然后分析如何让LLM根据用户需求提取出关键信息，让LLM根据用户提供的关键信息选择合适的租房API接口，调用接口时输入的参数。构造调用LLM时输入的prompt，使得LLM能够返回结构化的信息（API接口、参数等）。实现根据LLM返回的结构化信息，调用租房API接口的功能。
agent具备上下文管理的能力，对于同一个session_id的多轮会话可以存储上下文记忆，基于记忆内容调用LLM和租房API。
用例中的expected内容被判题器作为判分的依据，检查当前方案能否得到高分
把agent执行过程中调用API的信息记录下来，以便迭代分析agent执行过程中的问题，每次保存的日志文件名固定，并把上次记录的日志改名备份
当前环境无法访问所有的API接口，请基于样例生成本地测试用例与测试，并进行本地测试
确认当前所有修改，并推到github